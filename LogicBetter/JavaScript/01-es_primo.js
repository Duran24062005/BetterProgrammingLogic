for (let index = 0; index <= 60; index++) {
    if (index % 2 == 0) {
        console.log(`El numero: ${index} no es primo.`);
    } else {
        console.log(`El numero: ${index} es primo.`);
    }
    
}